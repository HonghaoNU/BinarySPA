% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-assign_clear_labels.R
\name{binary_spa_assign_labels_delta}
\alias{binary_spa_assign_labels_delta}
\title{Assign Binary-SPA labels using normalized top1-top2 margin (delta)}
\usage{
binary_spa_assign_labels_delta(
  score_mat,
  delta_thresh = 0.15,
  write_prefix = NULL,
  verbose = TRUE
)
}
\arguments{
\item{score_mat}{Score matrix (cells x cell_types).}

\item{delta_thresh}{Threshold for delta (default 0.15).}

\item{write_prefix}{If not NULL, write CSV outputs.}

\item{verbose}{Print messages.}
}
\value{
list(annot_final, cell_annotations, norm_scores)
}
\description{
Takes a score matrix (cells x cell_types) and assigns each cell the top
cell type if delta >= delta_thresh; otherwise marks "Mixed".
}
\details{
Cell types with all-zero scores are automatically removed.
}
